# MCNLM Python Implementation

## Running instructions
We are using **poetry**.

```bash
# install poetry
pip install --user poetry

# project dir
cd mcnlm

# install deps
poetry install

# activate environment
poetry env activate

# run
poetry run mcnlm
```